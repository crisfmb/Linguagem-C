#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
    /*srand(time(NULL));
    int n = rand() % 50 + 1; //Gera numero entre 1 e 50
    printf("Eu gerei o número: (%d)", n);*/

    return 0;
}